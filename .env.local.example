# Cloudinary (required for photo uploads)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=davterbwx

# Log form / portal password (defaults to "garden2025" if not set)
LOG_PASSWORD=2303

# Vercel Blob (auto-set when Blob store is connected in Vercel dashboard)
# BLOB_READ_WRITE_TOKEN=vercel_blob_...
